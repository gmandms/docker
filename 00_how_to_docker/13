# 13. Распечатайте переменные среды контейнера нерестового пула в одной команде, чтобы убедиться, что вы правильно настроили свой контейнер.

docker inspect --format '{{ .Config.Env }}' spawning-pool
